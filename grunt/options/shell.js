module.exports = function(grunt) {

    var pkg = grunt.file.readJSON('./package.json');
    var variants;

    var result = {
        options: {
            stdout: true,
            stderr: true,
            // stdin:  true,
        },
    };
    return result;
};
